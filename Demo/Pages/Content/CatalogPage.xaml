<Page x:Class="Demo.Pages.Content.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Demo.Pages.Content"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CatalogPage" Loaded="Page_Loaded" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBox x:Name="TbSerch" Width="150" Height="30" Padding="5.5" Margin="0 0 10 0" TextChanged="TbSerch_TextChanged"/>
            <ComboBox x:Name="CbSortBy" Width="150" Height="30" Margin="0 0 10 0" SelectionChanged="CbSortBy_SelectionChanged">
                <ComboBoxItem Content="По возрастанию"/>
                <ComboBoxItem Content="По убыванию"/>
            </ComboBox>
            <ComboBox x:Name="CbClassification" Width="150" Height="30" SelectionChanged="CbClassification_SelectionChanged">
                <ComboBoxItem Content="Все"/>
                <ComboBoxItem Content="Электронные курилочки"/>
                <ComboBoxItem Content="Снюсики"/>
                <ComboBoxItem Content="Славянские жижики"/>
                <ComboBoxItem Content="Испрители заморские"/>
            </ComboBox>
        </StackPanel>
        <ListView x:Name="LvCatalog" ScrollViewer.HorizontalScrollBarVisibility="Hidden" BorderThickness="0" Grid.Row="1">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="Black" Padding="10" CornerRadius="10" Width="200" Height="300">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Image Grid.Row="0" Source="{Binding Image}"/>
                            <TextBlock Grid.Row="1" Text="{Binding TovarName}" FontWeight="Bold"/>
                            <TextBlock Grid.Row="2" Text="{Binding Price}"/>
                            <TextBlock Grid.Row="3" Text="{Binding Description}"/>
                            <StackPanel Grid.Row="4">
                                <Button x:Name="BtnEdit" Content="Редактировать" FontWeight="Bold" Margin="0 10 0 5" Padding="10 5" Click="BtnEdit_Click" Visibility="{Binding AdminControllerVisibility}"/>
                                <Button x:Name="BtnDelete" Content="Удалить" Background="Red" FontWeight="Bold" Padding="10 5" Click="BtnDelete_Click" Visibility="{Binding AdminControllerVisibility}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
