<Page x:Class="Demo.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Demo.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"  BorderThickness="1" BorderBrush="Black" Padding="10" CornerRadius="10" Margin="12">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnBack" Grid.Column="1" Content="Назад" HorizontalAlignment="Right" Width="60" Height="25" FontWeight="Bold" Click="BtnBack_Click"/>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="BtnAdd" Content="Добавить" HorizontalAlignment="Center" Width="70" Height="30" FontWeight="Bold" Margin="0 0 10 0" Click="BtnAdd_Click"/>
                        <Button x:Name="BtnProfil" Content="Профиль"  Width="70" Height="30" FontWeight="Bold" Click="BtnProfil_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
        <Frame x:Name="ContentFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
